services:
  ashare:
    build: .
    container_name: ashare-review
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - TZ=Asia/Shanghai
      - DATA_DIR=/app/data
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env:ro
